# Simple-Chatbot
A simple rchatbot written in React that performs only three functions—flip a coin, get the current date, and roll a dice—built to practice React basics and styled using Tailwind CSS via CDN.
